Pull requests are how you merge branches on github

GitHub is a hub of gits .git directories

You can push and pull commits.

You an also clone to new computers/ paths

master branch is a lable that creates for when we initialize our repo

Head is another label that git greatws for us when we do our first commit

Head is not a branch

# creates a new branch (label) called git-notes
# shows us which branch is active
git branch
